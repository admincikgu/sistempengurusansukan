# V62.2 Rollback Fix

Base: V59 working build.

- Restored original working API/backend structure.
- Removed Student ID from Teacher registration UI and payload.
- New registrations receive an internal deterministic participant key only for legacy admin compatibility.
- Student ID is hidden from admin-facing UI, while existing internal admin operations remain compatible.
- No database records are deleted or migrated.
- All API/lib JavaScript files pass `node --check`.
